# Telegram Configuration (Optional - can also be configured via admin panel)
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Admin Password (Optional - only for initial setup)
# ADMIN_PASSWORD=your_secure_password_here

# Database (Will be provided by Replit/Neon)
# DATABASE_URL=postgresql://...

# Session Secret (Generate a random string)
SESSION_SECRET=change_this_to_random_secret_string

# Node Environment
NODE_ENV=production
PORT=5000
